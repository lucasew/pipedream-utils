{
  "name": "pipedream-utils",
  "version": "0.0.1",
  "description": "Some functions for pipedream scripts",
  "main": "lib/index.js",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage && tar -cf coverage.tar coverage && rm -rf coverage"
  },
  "author": "lucas59356 <lucas59356@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "jest": "^26.6.3"
  }
}
